{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "permessi di base dell'app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "notification:default",
    "updater:default",
    "process:default"
  ]
}
